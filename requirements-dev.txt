# Install: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Linting & format
ruff>=0.4,<0.7

# Pre-commit (optional)
pre-commit>=3.6,<4

# E2E (optional): pip install playwright && playwright install chromium
playwright>=1.40,<2

# Type checking (optional)
# mypy>=1.8,<2

# Testing (already in main requirements; pin if you want)
# pytest>=7.4
# pytest-asyncio>=0.23
# httpx>=0.26
